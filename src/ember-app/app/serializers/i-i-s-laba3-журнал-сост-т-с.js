import { Serializer as ЖурналСостТСSerializer } from
  '../mixins/regenerated/serializers/i-i-s-laba3-журнал-сост-т-с';
import __ApplicationSerializer from './application';

export default __ApplicationSerializer.extend(ЖурналСостТСSerializer, {
  /**
  * Field name where object identifier is kept.
  */
  primaryKey: '__PrimaryKey'
});
